# This file was generated by FRUT's Jucer2CMake from "LondonClockT.jucer"

cmake_minimum_required(VERSION 3.15)

project("LondonClockT")

option (USE_SYSTEM_JUCE "Use JUCE found via CMake rather than the bundled one." OFF)
option (ENABLE_LV2 "Add LV2 format if JUCE supports it" OFF)

if (USE_SYSTEM_JUCE)
    find_package(JUCE CONFIG REQUIRED)
else()
    add_subdirectory ("JUCE")
endif()

juce_add_plugin(LondonClockT
  VERSION "1.0.0"
  BUNDLE_ID "com.everything60.LondonClockT"
  COMPANY_NAME "morukutsu"
  COMPANY_COPYRIGHT "morukutsu"

  FORMATS
    "VST3"
    "Standalone"
    if (ENABLE_LV2)
    "LV2"
    LV2_URI com.everything60.LondonClockT
    endif()

  PLUGIN_NAME "LondonClockT"
  DESCRIPTION "LondonClockT"
  PLUGIN_MANUFACTURER_CODE "E60V"
  PLUGIN_CODE "S2cy"
  NEEDS_MIDI_INPUT TRUE
  NEEDS_MIDI_OUTPUT TRUE
  IS_MIDI_EFFECT TRUE
  AAX_IDENTIFIER "com.everything60.LondonClockT"
  AU_EXPORT_PREFIX "LondonClockTAU"
)

juce_generate_juce_header(LondonClockT)

target_compile_definitions(LondonClockT
  PUBLIC
    JUCE_QUICKTIME=disabled
    JUCE_USE_CURL=0
    JUCE_WEB_BROWSER=0
)

target_sources(LondonClockT
  PRIVATE
    "Source/Clocking.cpp"
    "Source/Gui.cpp"
    "Source/PluginEditor.cpp"
    "Source/PluginProcessor.cpp"
)

target_link_libraries(LondonClockT
  PRIVATE
    juce::juce_audio_basics
    juce::juce_audio_devices
    juce::juce_audio_formats
    juce::juce_audio_plugin_client
    juce::juce_audio_processors
    juce::juce_audio_utils
    juce::juce_core
    juce::juce_data_structures
    juce::juce_events
    juce::juce_graphics
    juce::juce_gui_basics
    juce::juce_gui_extra
  PUBLIC
    juce::juce_recommended_config_flags
    juce::juce_recommended_lto_flags
    juce::juce_recommended_warning_flags
)
